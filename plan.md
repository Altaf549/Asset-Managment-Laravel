Asset Management dashboard open with login credential no registration
all sidebar of this lit and in list setion open add button use of all page for add and edit use modal, and list uing datatable(search and sort)
    1. Laptop
        a) List (Timestamp, ASSET ID, Serial Number, Model NAME, MANUFACTURER, Screen Size, RAM, RAM Model, RAM FSB, SSD, HARD DISK, Processor Company, Processor, Processor Generation, Motherboard, Motherboard Model, ASSIGNED TO, PURCHASE DATE, VENDOR NAME, PURCHASE TYPE, EMP ID, statuAsset(toggle button) and action button(Edit, if alreday assign then show unassign button other wise assign button))
        b) Assign History
        c) Unassign Hesteroy
    2. Cpu
        a) List (Timestamp, ASSET ID, Cabinet NAME, RAM, RAM Model, RAM FSB, SSD, HARD DISK, Processor Company, Processor, Processor Generation, Motherboard, Motherboard Model, ASSIGNED TO, PURCHASE DATE, VENDOR NAME, PURCHASE TYPE, EMP ID, statuAsset(toggle button) and action button(Edit, if alreday assign then show unassign button other wise assign button))
        b) Assign History
        c) Unassign Hesteroy
    3. Mac
        a) List (Timestamp, ASSET ID, Serial Number, Cabinet NAME, RAM, RAM Model, RAM FSB, SSD, HARD DISK, Processor Company, Processor, Processor Generation, Motherboard, Motherboard Model, ASSIGNED TO, PURCHASE DATE, VENDOR NAME, PURCHASE TYPE, EMP ID, statuAsset(toggle button) and action button(Edit, if alreday assign then show unassign button other wise assign button))
        b) Assign History
        c) Unassign Hesteroy
    4. Monitor
        a) List(Timestamp, ASSET ID, MANUFACTURER, Screen Size, Resolution, HDMI or VGA,  ASSIGNED TO, PURCHASE DATE, VENDOR NAME, PURCHASE TYPE, EMP ID, statuAsset(toggle button) and action button(Edit, if alreday assign then show unassign button other wise assign button))
        b) Assign History
        c) Unassign Hesteroy
    
    5. Keyboard
        a) List(Timestamp, ASSET ID, MANUFACTURER, Keyboard Type(Wierd, Blutooth), ASSIGNED TO, PURCHASE DATE, VENDOR NAME, PURCHASE TYPE, EMP ID, statuAsset(toggle button) and action button(Edit, if alreday assign then show unassign button other wise assign button))
        b) Assign History
        c) Unassign Hesteroy
    6. Mouse
        a) List(Timestamp, ASSET ID, MANUFACTURER, Mouse Type(Wierd, Blutooth), ASSIGNED TO, PURCHASE DATE, VENDOR NAME, PURCHASE TYPE, EMP ID, statuAsset(toggle button) and action button(Edit, if alreday assign then show unassign button other wise assign button))
        b) Assign History
        c) Unassign Hesteroy

    7. Other asset
        a) List (Title, aset Id)
        b) Assign History
        c) Unassign Hesteroy
